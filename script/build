#!/usr/bin/env bash
# script/build — produce one .tbz tarball per bundle. Wraps build.rb so it
# runs on the Ruby this project pins.
set -euo pipefail

cd "$(dirname "$0")/.."

# shellcheck source=script/_ruby.sh
source "$(dirname "$0")/_ruby.sh"

exec rv run ruby script/build.rb
