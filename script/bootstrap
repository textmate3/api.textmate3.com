#!/usr/bin/env bash
# script/bootstrap — install Ruby gem dependencies for api.textmate3.com
set -euo pipefail

cd "$(dirname "$0")/.."

# shellcheck source=script/_ruby.sh
source "$(dirname "$0")/_ruby.sh"

exec rv run bundle install
